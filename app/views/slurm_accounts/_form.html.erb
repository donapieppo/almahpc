<%= dm_form_for slurm_account do |f| %>
  <%= f.dm_error_notification %>  
  <%= f.input :name %>
  <%= f.input :slug %>
  <div class="my-2 offset-sm-3">
    <%= f.rich_text_area :description %>
  </div>
  <%= f.submit %>
<% end %>
